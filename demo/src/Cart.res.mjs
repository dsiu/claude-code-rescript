// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Product from "./Product.res.mjs";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";

function make(cartId, userId, itemsOpt) {
  let items = itemsOpt !== undefined ? itemsOpt : [];
  return {
    cartId: cartId,
    userId: userId,
    items: items
  };
}

function addItem(cart, product) {
  return {
    cartId: cart.cartId,
    userId: cart.userId,
    items: cart.items.concat([product])
  };
}

function removeItem(cart, productId) {
  return {
    cartId: cart.cartId,
    userId: cart.userId,
    items: cart.items.filter(item => item.id !== productId)
  };
}

function calculateTotal(cart) {
  return Stdlib_Array.reduce(cart.items, 0.0, (acc, product) => acc + Product.getPrice(product));
}

function getItemCount(cart) {
  return cart.items.length;
}

function isEmpty(cart) {
  return cart.items.length === 0;
}

export {
  make,
  addItem,
  removeItem,
  calculateTotal,
  getItemCount,
  isEmpty,
}
/* No side effect */
