// Generated by ReScript, PLEASE EDIT WITH CARE


function makeUser(firstName, lastName, age, email) {
  return {
    firstName: firstName,
    lastName: lastName,
    age: age,
    email: email
  };
}

function makeUserWithOptionalAge(firstName, lastName, age, email) {
  return {
    firstName: firstName,
    lastName: lastName,
    age: age,
    email: email
  };
}

function getFullName(user) {
  return user.firstName + " " + user.lastName;
}

function formatFullName(user) {
  return user.firstName + " " + user.lastName;
}

export {
  makeUser,
  makeUserWithOptionalAge,
  getFullName,
  formatFullName,
}
/* No side effect */
