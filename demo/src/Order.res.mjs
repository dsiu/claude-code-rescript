// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Product from "./Product.res.mjs";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";

function make(orderId, userId, products) {
  return {
    orderId: orderId,
    userId: userId,
    products: products,
    total: 0.0
  };
}

function calculateTotal(order) {
  return Stdlib_Array.reduce(order.products, 0.0, (acc, product) => acc + Product.getPrice(product));
}

export {
  make,
  calculateTotal,
}
/* No side effect */
